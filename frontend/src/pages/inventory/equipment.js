import React from 'react'
import InventoryList from '@/components/Layouts/Inventory/InventoryList'

const Equipment = () => {
    return <InventoryList title={'Equipment'} />
}

export default React.memo(Equipment)
